# Scope resolution: 'local' lives in outer() and is NOT visible in other().
create shared = 10

function outer with param
  create local = param
  show shared
  show local
end

function other
  show shared
  show local
end
